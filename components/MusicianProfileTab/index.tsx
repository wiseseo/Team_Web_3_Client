import * as React from "react";
import styled from "styled-components";

interface Props {}
const Container = styled.div`
  color: white;
  flex: 5;
  display: flex;
`;
const MusicianProfileTab = (props: Props) => {
  return <Container>탭</Container>;
};

export default MusicianProfileTab;
